# Environment-specific configuration for cloud-init generation
# Copy this file to dev.env (or test.env, prod.env) and fill in values

# Environment name (used in hostname: dokploy-${ENVIRONMENT})
ENVIRONMENT=dev

# Tailscale auth key (generate at https://login.tailscale.com/admin/settings/keys)
# Use reusable, ephemeral keys for dev/test; single-use for prod
TAILSCALE_AUTH_KEY=tskey-auth-XXXXXXXXXXXX-XXXXXXXXXXXXXXXXXXXXXXXX

# Docker Swarm default address pool
# Avoid overlap with Azure VNet (typically 10.x.x.x)
# dev: 172.20.0.0/16, test: 172.21.0.0/16, prod: 172.22.0.0/16
DOCKER_SWARM_ADDR_POOL=172.20.0.0/16
